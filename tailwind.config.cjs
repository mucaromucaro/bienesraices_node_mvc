//firts we have to write in the console npx tailwindcss init -p to be able see this archive
// then we have to modific to be able apply the css o styles

//if the code is good write and  no working. is because we have to add the letter c between the j and the point

module.exports = {
  content: ["./views/**/*.pug"],//this is to faind all archive.pug and apply the style
  theme: {
    extend: {},
  },
  plugins: [],
}

